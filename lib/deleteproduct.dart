import 'package:flutter/material.dart';

class DeleteProductView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
        child: Text('Delete the Products'),
      );
  }
}
